rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // ⚠️ TEMPORARY: OPEN RULES FOR DEVELOPMENT
    // TODO: Restrict these rules before production
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}